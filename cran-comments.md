## R CMD check results

Local check using `devtools::check(args = "--as-cran")`:

0 errors | 0 warnings | 0 notes

Windows check using `devtools::check_win_devel()` on R-devel:

0 errors | 0 warnings | 1 note

The only note is:

* New submission

## Submission notes

This is a new submission.

The package provides tools for document ingestion, local embedding storage, retrieval-augmented generation, QA logging, and LLM-scored RAGAS-style evaluation in R.

Examples and tests do not require an OpenAI API key or internet access. Tests use synthetic data and temporary files where needed.
